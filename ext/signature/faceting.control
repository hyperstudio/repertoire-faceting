# Faceting PostgreSQL extension module
# Bundles up the signature C-level data type
 
comment = 'A bitset index data type for faceted indexing and queries'
default_version = '0.6.0'
relocatable = true